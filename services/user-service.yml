server:
    port: 0

eureka:
  instance:
    instanceId: ${spring.application.name}:${vcap.application.instance_id:${spring.application.instance_id:${random.value}}}

spring:
    datasource:
        url: jdbc:mysql://localhost:3306/elibrary_users?createDatabaseIfNotExist=true&useSSL=false&allowPublicKeyRetrieval=true
        username: root
        password: z;=4QCLG%e>gHY$u
        driver-class-name: com.mysql.cj.jdbc.Driver
    jpa:
        hibernate:
            ddl-auto: update # Automatically creates tables for you!
        show-sql: true

  