eureka:
  client:
    service-url:
       defaultZone: http://localhost:8761/eureka/

management:
  endpoints:
    web:
      exposure:
        include: "*"

logging:
  level:
    com.mahesha: DEBUG
    org.springframework.web: INFO
    org.hibernate.sql: DEBUG